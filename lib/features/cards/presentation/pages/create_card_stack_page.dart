import 'package:flutter/material.dart';

class CreateCardStackPage extends StatelessWidget {
  const CreateCardStackPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
